import { Fragment } from "react";
import header from "./Header.module.css"
function Welcome(props) {
    return (
    <header className={header.header}> TAST MANAGER</header>
    );
}

export default Welcome